#include <stdio.h>

#include <stdlib.h>

void NhapMT(int,int,int**);
void XuatMT(int,int,int**);

int main()
{
  /* int i,m,n,**p;
   printf("\n->Nhap so dong: ");
   scanf("%d", &m);
   printf("->Nhap so cot: ");
   scanf("%d", &n);
   p=(int*)malloc(m*sizeof(int));

   for(i=0;i<m;i++){
   p=(int*)malloc(n*sizeof(int));
   }
   printf("\n");
   NhapMT(m,n,p);
   printf("\n");
   XuatMT(m,n,p);
*/

   return 0;​
}
